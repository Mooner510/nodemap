name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  pipeline:
    name: CI
    uses: Mooner510/workflows/.github/workflows/pipeline.yml@4f579537d18893be2dee87640be35d7f91bb66c8
    with:
      components: |
        [
          {
            "name": "android",
            "type": "java-kotlin",
            "path": ".",
            "profile": "android"
          }
        ]
